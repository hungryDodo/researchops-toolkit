{
  "schema_version": 2,
  "purpose": "Non-secret adapter templates. Verify each provider's current official documentation before enabling a model.",
  "recipes": [
    {
      "id": "openai-compatible",
      "protocol": "openai-chat-compatible",
      "required_fields": ["provider", "model", "base_url", "credential_env"],
      "optional_fields": ["endpoint_id", "headers", "chat_path", "model_revision", "deployment_epoch"],
      "secret_policy": "credential_env only; never store the value in project state",
      "competence_policy": "probe/smoke updates endpoint and identity telemetry only"
    },
    {
      "id": "local-openai-compatible",
      "protocol": "openai-chat-compatible",
      "required_fields": ["provider", "model", "base_url"],
      "optional_fields": ["checkpoint_hash", "quantization", "tensor_parallel", "tool_schema_revision"],
      "secret_policy": "local endpoints may omit credentials; do not assume local implies trusted data handling",
      "competence_policy": "record each materially different inference configuration as a separate execution arm"
    }
  ]
}
