# Execution invariants

Raw inputs and outputs are append-only. Derived artifacts may be regenerated. Every reported aggregate lists all eligible run IDs and exclusion reasons. A successful process exit is not a scientifically valid run until schema and protocol checks pass.
