# Reproduction separation

Use a new run namespace and separate ledger evidence. Author runs cannot count as reproduction. If the same person operates both roles, preserve the information barrier and document it as a limitation.
