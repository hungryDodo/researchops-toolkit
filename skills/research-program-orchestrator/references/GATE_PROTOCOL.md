# Independence

For high-stakes gates, use a fresh context or a separate Agent. The reviewer reads authoritative artifacts, not optimistic progress summaries. A gate can downgrade or delete proposed claims, but cannot fabricate missing experiments.
