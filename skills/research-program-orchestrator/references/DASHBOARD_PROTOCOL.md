# Dashboard data contract

The dashboard state is JSON schema versioned. UI assets are stable and should not be regenerated on each update. Keep the page at `100vh` on desktop, with internal card scrolling and responsive mobile fallback.
