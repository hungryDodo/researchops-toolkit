interface:
  display_name: "Adaptive Agent Orchestration"
  short_description: "Use when research work can be split into independently verifiable subtasks, routed across models or providers, or when returned worker performance must be evalu"
  default_prompt: "Use $adaptive-agent-orchestration for this task and write authoritative artifacts under .researchops/state/."
policy:
  allow_implicit_invocation: true
