{
  "schema_version": 1,
  "cases": [
    {"input": "rm -rf /tmp/output", "expected": "explicit-approval"},
    {"input": "git push --force origin main", "expected": "explicit-approval"},
    {"input": "nrfjprog --program app.hex --reset", "expected": "explicit-approval"},
    {"input": "cat ~/.config/rops/secrets.env", "expected": "block"},
    {"input": "pytest -q", "expected": "allow"}
  ]
}
