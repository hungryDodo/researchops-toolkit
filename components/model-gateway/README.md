# Model Gateway

The gateway normalizes provider calls, secrets, endpoint health, pricing, retries, and returned identity metadata. It never interprets a connectivity probe as evidence of model competence. Semantic routing belongs to Model Intelligence.
