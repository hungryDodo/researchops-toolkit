# Status model

Claims: proposed → exploratory → supported → independently_verified → approved_for_writing → superseded/rejected.

Evidence: registered → validated → invalidated. Validation means the artifact exists, checksum matches, provenance is complete, and the analysis protocol is known; it does not automatically mean the scientific interpretation is correct.
